# ===============================================
# SECURITY WARNING: NEVER COMMIT THIS FILE
# ===============================================
# This file contains sensitive credentials
# Keep .env in .gitignore at all times
# Use CI/CD secrets for production

# Google Drive API Configuration
# Follow setup instructions in CLAUDE-assets/GOOGLE_DRIVE_SETUP.md

# Main Google Drive folder ID (extract from folder URL)
# Example: https://drive.google.com/drive/folders/1ABC123xyz -> use "1ABC123xyz"
# REQUIRED if using Google Drive provider
GOOGLE_DRIVE_FOLDER_ID=

# Language-specific folder IDs (optional, for separate folders per language)
GOOGLE_DRIVE_FOLDER_ID_AR=
GOOGLE_DRIVE_FOLDER_ID_EN=
GOOGLE_DRIVE_FOLDER_ID_TR=
GOOGLE_DRIVE_FOLDER_ID_UR=

# Service Account Credentials (SENSITIVE - KEEP SECRET)
# REQUIRED if using Google Drive provider
# Option 1: Direct JSON string (recommended for CI/CD)
# Get this from Google Cloud Console -> Service Accounts -> Keys
GOOGLE_DRIVE_CREDENTIALS='{"type":"service_account","project_id":"YOUR_PROJECT","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"your-service-account@project.iam.gserviceaccount.com"}'

# Option 2: Path to credentials file (recommended for local development)
# GOOGLE_DRIVE_CREDENTIALS_PATH=./google-credentials.json

# Google Docs Export Format
# Options: 'markdown' or 'html' (markdown recommended)
GOOGLE_DOCS_EXPORT_FORMAT=markdown

# Public URL of this deploy, subpath included. Required for a build; the subpath
# becomes Astro's base, so https://example.com/docs mounts at /docs.
# SITE_URL=https://example.com

# Keeps a deploy out of search results. Staging and previews only.
# NOINDEX=1

# Overrides the base derived from SITE_URL. Only needed behind a proxy.
# BASE_PATH=

# Analytics Configuration
# Google Tag Manager Container ID (format: GTM-XXXXXXX)
# Leave empty to disable GTM tracking
# Get this from: https://tagmanager.google.com/
PUBLIC_GTM_ID=

# Build Configuration
NODE_ENV=development
